[pytest]
addopts = --strict-markers
testpaths = tests
markers =
    unit: unit tests
    integration: integration tests
    contract: API/schema contract tests
    e2e: end-to-end tests
    slow: slow-running tests
    external: tests requiring external services or network
