# Legacy Templates Archived

This directory is archived and no longer served at runtime.

## Policy

- Default UI entry is `frontend-modern`.
- Backend routes now return modern redirect or `410 Legacy UI archived` response.
- Files are kept only for audit/reference and short-term rollback safety.
- Do not add new features here; modern frontend is the active development target.

