## Scope
- Summary of change:
- In scope modules/files:
- Out of scope:
- Closure topic / task id:

## Risk
- Risk level: Low / Medium / High
- Main failure modes:
- User or system impact if this fails:
- Compatibility / rollout guard kept:

## Test Evidence
- Required checks affected:
- Automated tests (commands + results):
- Manual verification (steps + results):
- Monitoring/metrics checked:

## Semantic Movement Completeness
- Semantic movement inventory path (legacy/donor -> target):
- `UNASSIGNED_BLOCKER` count: 0 / >0
- Legacy end-to-end trace:
- Target end-to-end trace + structure-preservation check:
- Failure / reverse-return case traced:
- Declared loss(s) or zero-loss declaration:
- Unwired / contract-only capabilities disposition (placed or rejected):
- Does this change include migration/refactor/successor/backend replacement/code generation? Yes / No

## Mechanical Routing And Review
- Mechanical worker/model route used (e.g., DeepSeek for fixed-IO implementation; None if no mechanical worker):
- Package boundary (target / input / output / allowed read-write / acceptance):
- Mainline reviewer responsible for architecture, semantic inventory/matrix, risk acceptance, promotion, and final review:
- Worker returned result / changed files / verification / risk:
- Frozen semantics changed by worker? Yes / No
- Mechanical generation passed semantic movement completeness gate? Yes / No / N/A

## Rollback Strategy
- Revert method:
- Data/schema rollback needed: Yes / No
- Safe rollback window and owner:

## Docs And Closure
- Dev docs updated:
- Indexes updated: Yes / No
- Closure / validation note path:
