# Local Index Backend Evaluation

This run creates a MRW latest-dev-docs dataset and benchmarks available local candidates. External clients are used only when present on PYTHONPATH, without adding project dependencies.
